spring:
  application:
    name: service-verification
  cloud:
    nacos:
      config:
        file-extension: yaml
        server-addr: @nacos.server.addr@
        namespace: @nacos.server.namespace@